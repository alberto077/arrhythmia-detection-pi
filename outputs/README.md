# Generated Outputs

This directory is reserved for local experiment outputs. Generated plots,
sweep CSVs, NumPy arrays, and local model variants should stay out of Git unless
they are intentionally promoted into `docs/analysis/` or `models/`.

Common generated artifacts:

```text
threshold_trace_*.png
sweep_results*.csv
*.npy
*.npz
```
